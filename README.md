# EPUB Translator 📚 <=> 🌐

Простое веб-приложение для перевода книг в формате EPUB с использованием Google Gemini API или OpenRouter API. Загрузите свою книгу, выберите язык и модель, переводите по главам или все сразу, а затем скачайте результат в виде текста или нового EPUB файла.

## ✨ Возможности

*   **Загрузка EPUB:** Простой интерфейс для загрузки файлов `.epub`.
*   **Автоматический парсинг:** Извлечение структуры книги и оглавления (TOC).
*   **Перевод оглавления:** Автоматический перевод названий глав в TOC при загрузке книги.
*   **Выбор API и модели:**
    * Поддержка Google Gemini API и OpenRouter API
    * Возможность выбрать целевой язык перевода
    * Выбор конкретной модели (список загружается динамически с учетом фильтрации, например, по стоимости для OpenRouter)
*   **Выбор типа операции:** Помимо перевода, доступны операции "Пересказ" и "Анализ трудностей".
*   **Фоновая обработка:** Обработка (перевод, пересказ, анализ) выполняется в фоновом режиме.
    *   Запуск для отдельных секций по клику.
    *   Запуск для всех необработанных секций одной кнопкой.
*   **Обновление статуса:** Отображение статуса для каждой секции (необработано, в процессе, обработано, ошибка, пустое) с указанием модели и обновлением в реальном времени. Общий статус книги также отражает прогресс всех секций.
*   **Просмотр результатов:** Отображение обработанного текста выбранной секции (перевод, пересказ, анализ) прямо в браузере.
*   **Кэширование:** Обработанные секции кэшируются на диске для ускорения загрузки и избежания повторных запросов.
*   **Скачивание результатов:**
    *   Скачивание обработанного текста отдельной секции в формате `.txt`.
    *   Скачивание всего обработанного текста книги (всех секций) в одном файле `.txt`.
    *   Скачивание обработанной книги в виде нового, упрощенного файла `.epub`.
*   **Управление книгами:** Удаление загруженных книг и их кэша с диска.

## 🚀 Технологический стек

*   **Бэкенд:** Python 3, Flask
*   **API Обработки:** Google Gemini API (`google-generativeai`), OpenRouter API
*   **Парсинг и генерация EPUB:** `ebooklib`
*   **Фронтенд:** HTML, CSS, JavaScript (без фреймворков)
*   **Асинхронность:** `concurrent.futures.ThreadPoolExecutor` для фоновых задач.

## 📋 Предварительные требования

*   Python 3 (рекомендуется 3.9+)
*   `pip` (менеджер пакетов Python)
*   **API ключи (один из):**
    * **Google API Key:** Активный ключ с доступом к Google Gemini API (Vertex AI или AI Studio). [Как получить ключ](https://aistudio.google.com/app/apikey)
    * **OpenRouter API Key:** Ключ доступа к OpenRouter API. [Как получить ключ](https://openrouter.ai/keys)

## ⚙️ Установка и настройка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <URL_вашего_репозитория>
    cd <папка_репозитория>
    ```

2.  **Создайте и активируйте виртуальное окружение** (рекомендуется):
    ```bash
    python -m venv venv
    # Linux/macOS
    source venv/bin/activate
    # Windows
    .\venv\Scripts\activate
    # Для PowerShell в Windows может потребоваться изменить политику выполнения:
    # Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте API ключ:**
    *   **ВАЖНО:** Никогда не добавляйте ваши API ключи напрямую в код или в систему контроля версий!
    *   **Выберите API для использования, установив переменную `TRANSLATOR_API`:**
        * `TRANSLATOR_API=google` для Google Gemini API (по умолчанию)
        * `TRANSLATOR_API=openrouter` для OpenRouter API
    *   **Установите соответствующий API ключ:**
        * Для Google Gemini API: `GOOGLE_API_KEY`
        * Для OpenRouter API: `OPENROUTER_API_KEY`
    *   **Рекомендуемый способ:** Установите переменные окружения
        *   **Linux/macOS:**
            ```bash
            export TRANSLATOR_API='google'  # или 'openrouter'
            export GOOGLE_API_KEY='ВАШ_GOOGLE_API_КЛЮЧ'
            # или
            export OPENROUTER_API_KEY='ВАШ_OPENROUTER_API_КЛЮЧ'
            ```
            (Добавьте эти строки в `~/.bashrc` или `~/.zshrc` для постоянной установки)
        *   **Windows (Command Prompt):**
            ```cmd
            set TRANSLATOR_API=google
            set GOOGLE_API_KEY=ВАШ_GOOGLE_API_КЛЮЧ
            :: или
            set TRANSLATOR_API=openrouter
            set OPENROUTER_API_KEY=ВАШ_OPENROUTER_API_КЛЮЧ
            ```
        *   **Windows (PowerShell):**
            ```powershell
            $env:TRANSLATOR_API="google"
            $env:GOOGLE_API_KEY="ВАШ_GOOGLE_API_КЛЮЧ"
            # или
            $env:TRANSLATOR_API="openrouter"
            $env:OPENROUTER_API_KEY="ВАШ_OPENROUTER_API_КЛЮЧ"
            ```
        *   **Альтернатива:** Использовать файл `.env` и библиотеку `python-dotenv`. Создайте файл `.env` в корне проекта:
            ```env
            TRANSLATOR_API=google  # или openrouter
            GOOGLE_API_KEY=ВАШ_GOOGLE_API_КЛЮЧ
            # или
            OPENROUTER_API_KEY=ВАШ_OPENROUTER_API_КЛЮЧ
            ```

## ▶️ Запуск приложения

1.  Убедитесь, что ваше виртуальное окружение активировано и переменная `TRANSLATOR_API` установлена, а также соответствующий API ключ (`GOOGLE_API_KEY` или `OPENROUTER_API_KEY`).
2.  Запустите Flask приложение:
    ```bash
    python app.py
    ```
3.  Откройте веб-браузер и перейдите по адресу `http://127.0.0.1:5000` (или адресу, указанному в консоли, если используется `host='0.0.0.0'`).

## 📖 Использование

1.  **Загрузка:** На главной странице нажмите "Выберите файл", выберите ваш `.epub` файл, выберите язык обработки по умолчанию и нажмите "Загрузить". Подождите, пока книга обработается (включая перевод оглавления).
2.  **Просмотр/Обработка:** На главной странице нажмите кнопку "Просмотр/Обработка" рядом с нужной книгой.
3.  **Настройка:** На странице книги выберите API, модель и **тип операции** (Перевод, Пересказ, Анализ). Выбор сохраняется в сессии.
4.  **Обработка секций:**
    *   Нажмите на название главы в оглавлении слева. Если результат обработки уже есть в кэше, он отобразится справа. Если нет, запустится фоновая обработка выбранной секции с выбранным типом операции и моделью.
    *   Нажмите кнопку "Обработать все непроведенные", чтобы запустить обработку всех секций без готового результата или с ошибками с текущими настройками API, модели и типа операции.
5.  **Мониторинг:** Следите за статусом обработки секций в оглавлении слева. Статусы (Необработано, В процессе, Переведено, Пересказано, Проанализировано, Ошибка: ..., Пустое) обновляются автоматически и включают название использованной модели. Общий статус книги также отображается сверху.
6.  **Просмотр результата:** Кликните на обработанную секцию, чтобы увидеть текст результата (перевод, пересказ или анализ) справа.
7.  **Скачивание:**
    *   Нажмите иконку `💾` рядом с обработанной секцией, чтобы скачать ее текст.
    *   Кнопки "Скачать весь текст (.txt)" и "Скачать EPUB" становятся активными, когда **все** секции книги имеют финальный статус (Переведено, Пересказано, Проанализировано, Пустое, Ошибка: ...).
8.  **Удаление:** На главной странице нажмите кнопку "Удалить" рядом с книгой, чтобы удалить ее файл и весь связанный кэш.

## 📁 Структура проекта
.

├── .epub_cache/ # Директория для кэшированных результатов обработки секций

├── .translated/ # (Опционально) Директория для полных обработанных файлов (если используется)

├── static/ # Статические файлы (CSS, JS)

│ └── js/

│ └── main.js # Логика фронтенда

├── templates/ # HTML шаблоны Flask

│ ├── index.html # Главная страница (загрузка, список книг)

│ └── book_view.html # Страница просмотра/обработки книги

├── uploads/ # Директория для загруженных EPUB файлов

├── app.py # Основной файл приложения Flask (маршруты, логика)

├── cache_manager.py # Функции для работы с кэшем

├── db_manager.py # Функции для работы с базой данных SQLite

├── epub_creator.py # Функции для генерации нового EPUB файла

├── epub_parser.py # Функции для парсинга EPUB (структура, текст, TOC)

├── translation_module.py # Функции для взаимодействия с API и логика обработки текста

├── requirements.txt # Список зависимостей Python

└── README.md # Этот файл


## ⚠️ Ограничения и Предупреждения

*   **Состояние в памяти:** Текущая информация о прогрессе обработки (`book_progress`) хранится в памяти Python. Перезапуск сервера Flask приведет к потере этого состояния (хотя кэш на диске и статус в БД останутся).
*   **Стоимость API:** Использование API может быть платным в зависимости от объема использования и выбранной модели. Следите за своей квотой и расходами.
*   **Качество результатов:** Качество обработки (перевода, пересказа, анализа) сильно зависит от выбранной модели и сложности исходного текста.
*   **Обработка ошибок:** Текущая обработка ошибок покрывает основные сценарии (включая ретраи при 429 ошибке и базовый парсинг ответов API), но может быть улучшена.
*   **Лимиты скорости API:** При одновременной обработке большого количества разделов возможно превышение лимитов скорости API. В текущей версии добавлена небольшая задержка после каждой обработанной секции в фоновой задаче. Требуется реализация централизованной очереди запросов для более надежного управления этим.